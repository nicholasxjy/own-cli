{
  "name": "own-cli",
  "version": "1.0.0",
  "description": "A cli tool for generating ownership for projects",
  "main": "./bin/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rimraf dist && microbundle watch",
    "build": "rimraf dist && microbundle"
  },
  "bin": "./bin/index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "@babel/generator": "^7.14.2",
    "@babel/parser": "^7.14.2",
    "@babel/template": "^7.12.13",
    "@babel/traverse": "^7.14.2",
    "@babel/types": "^7.14.2",
    "chalk": "^2.4.2",
    "commander": "^2.20.3"
  }
}
